name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
      
  
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1  # ✅ Updated
  firebase_messaging: ^15.2.4  # ✅ Updated
  flutter_tts: ^4.2.2
  speech_to_text: ^7.0.0
  google_maps_flutter: ^2.10.1
  geolocator: ^13.0.3  # ✅ Updated
  flutter_localization: ^0.3.1  # ✅ Updated
  fl_chart: ^0.70.2
  provider: ^6.1.2
  dio: ^5.8.0+1
  shared_preferences: ^2.2.2  # ✅ Updated
  cloud_firestore: ^5.6.5  # ✅ Updated
  firebase_auth: ^5.5.1  # ✅ Updated
  permission_handler: ^11.4.0
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
